{% extends "base.html.twig" %}
{% block title %}Form{% endblock %}
{% block body %}
    {{form_start(form)}}
    {{form_widget(form)}}
    {{form_end(form)}}
    {% for message in app.flashes('type') %}
    {{ message }}
    {% endfor %}
{% endblock %}